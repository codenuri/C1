#include <stdio.h>

int main(void)
{
    int num; // 정수를 보관할
             // 4바이트의 메모리를
             // 할당하고
             // 이름을 num 으로 사용.
    
    num = 10;
    
    printf("num = %d\n", num);    
    
    return 0;
}
